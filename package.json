{
  "name": "@vlegm/utils",
  "version": "1.0.1",
  "description": "Collection of utility functions used for vlegm projects",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepublishOnly": "yarn build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.7.1",
    "@types/yaml": "^1.9.7",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "yaml": "^1.10.2"
  }
}
